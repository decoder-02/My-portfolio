# Example .env for local development
# Copy this to a file named .env and fill in values before running the server.

# Server
PORT=5000

# SMTP (optional) - when set, the contact form will send real email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=akashrajrad@gmail.com
SMTP_PASS=xdkz qqry zwpd lupd
SMTP_SECURE=false

# Where inbound contact messages should be delivered (optional - defaults to SMTP_USER)
CONTACT_EMAIL=akashrajrad@gmail.com
